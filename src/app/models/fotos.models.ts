import { SafeResourceUrl } from '@angular/platform-browser';

export class Images {
    public albumId: number;
    public id: number;
    public title: string;
    public url: string;
    public thumbnailUrl: string;
};